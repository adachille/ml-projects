[project]
name = "chessmate"
version = "0.1.0"
description = "AlphaZero-lite: Train a neural network to play chess"
requires-python = ">=3.10"
dependencies = [
    "python-chess",
    "torch",
    "numpy",
    "matplotlib",
    "tqdm",
    "mlflow",
]
